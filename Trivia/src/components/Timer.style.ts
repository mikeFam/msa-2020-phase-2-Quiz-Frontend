import styled from 'styled-components';




export const Wrapper = styled.div`

    section {
        color: #ffff;
        display: flex;
        letter-spacing:2px;
    }

    .time-container span {
        position: relative;
        top: 5px;
        font-size:4rem;
    } 

    .timer-text {
        margin: 0;
        font-size:5rem;
    }
`;
